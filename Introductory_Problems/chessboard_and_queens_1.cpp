#import<ios>
#define g ,f[n]|=getchar()<43)
using h = uint8_t;
h s,n,q,f[8];
void S(h l, h r, h c, h i)
{
    s+=i>7;
    for(h H=~(l|r|c|f[i]),R;H;S(2*(l|R),(r|R)/2,c|R,i+1))H^=R=-H&H;
}
int main()
{
    for(;n<8;++n g for(q=8;q--;f[n]*=2 g;
    S(0,0,0,0);
    printf("%d",s);
}